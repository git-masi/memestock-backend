createAiProfile:
  handler: src/handlers/createAiProfile.handler
  events:
    - http:
        method: POST
        path: /ai/create

executeAiAction:
  handler: src/handlers/executeAiAction.handler
  # events:
  #   - schedule: rate(30 seconds)
