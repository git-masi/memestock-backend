TransactionsTableIAM:
  Effect: Allow
  Action:
    - dynamodb:Scan
    - dynamodb:Query
    - dynamodb:PutItem
    - dynamodb:GetItem
    - dynamodb:UpdateItem
  Resource:
    - !GetAtt TransactionsTable.Arn
    - !Join ['/', [!GetAtt TransactionsTable.Arn, 'index', '*']]
